import 'package:flutter/material.dart';

class ContainerBridgeScreen extends StatelessWidget {
  const ContainerBridgeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement container bridge management
    return const Scaffold(
      body: Center(child: Text('ContainerBridgeScreen')),
    );
  }
}
