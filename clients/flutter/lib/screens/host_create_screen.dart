import 'package:flutter/material.dart';

class HostCreateScreen extends StatelessWidget {
  const HostCreateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement host creation form
    return const Scaffold(
      body: Center(child: Text('HostCreateScreen')),
    );
  }
}
