# Keeps scripts/testing/secrets/ present in git while every real secret file in
# it stays untracked (see the .gitignore feature-tier-credentials block). Do NOT
# place real credentials here in a tracked file — Constitution §11.4.10.
