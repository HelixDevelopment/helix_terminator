apiVersion: v2
name: helixterm
description: Umbrella Helm chart for HelixTerminator

type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  # TODO: add subchart dependencies for each service
