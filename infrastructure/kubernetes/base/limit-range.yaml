apiVersion: v1
kind: LimitRange
metadata:
  name: helixterminator
  namespace: helixterminator
spec:
  limits:
    - default:
        memory: 512Mi
        cpu: 500m
      defaultRequest:
        memory: 256Mi
        cpu: 100m
      type: Container
