apiVersion: v1
kind: Service
metadata:
  name: health-service
  namespace: helixterminator
  labels:
    app.kubernetes.io/name: health-service
    app.kubernetes.io/component: service
    app.kubernetes.io/part-of: helixterminator
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: health-service
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
