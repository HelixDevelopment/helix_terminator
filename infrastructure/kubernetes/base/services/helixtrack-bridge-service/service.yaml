apiVersion: v1
kind: Service
metadata:
  name: helixtrack-bridge-service
  namespace: helixterminator
  labels:
    app.kubernetes.io/name: helixtrack-bridge-service
    app.kubernetes.io/component: service
    app.kubernetes.io/part-of: helixterminator
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: helixtrack-bridge-service
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
